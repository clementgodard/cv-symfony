{% extends 'admin/base.html.twig' %}

{% block title %}Liste des catégories{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('liste') }}
{% endblock %}

{% block body %}
    {{ parent() }}

    {% from 'macro/categorie.html.twig' import recursiveCategorieAdmin %}
    <main id="liste" class="container">
        {% for categorie in categories %}
            {{ recursiveCategorieAdmin(categorie, 1, nbRootCategories-1) }}
        {% endfor %}
    </main>
{% endblock %}

